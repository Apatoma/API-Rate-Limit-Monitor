import React from 'react';

const AlertSettings = () => {
    // Placeholder component for alert settings
    return (
        <div>
            <h2>Alert Settings</h2>
            <p>Configure alert thresholds here...</p>
        </div>
    );
};

export default AlertSettings;
